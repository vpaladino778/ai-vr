# GPU dependencies for RunPod (RTX 4090 / CUDA 11.8)
#
# Base image runpod/pytorch:2.1.0-py3.10-cuda11.8.0-devel-ubuntu22.04 already
# provides torch==2.1.0+cu118 and torchvision, so we don't reinstall them here.
#
# xformers 0.0.22.post7 is the correct build for torch 2.1.0 + CUDA 11.8:
xformers==0.0.22.post7
diffusers==0.29.2
transformers==4.42.3
accelerate==1.0.1
safetensors==0.4.3

# Video I/O (StereoCrafter)
decord==0.6.0
av==12.3.0

# Core pipeline deps (same as requirements-cpu.txt minus torch/torchvision)
opencv-python==4.10.0.84
pillow
numpy
pyyaml
scipy

# Weight downloader
huggingface_hub

# StereoCrafter CLI helper
fire==0.5.0
